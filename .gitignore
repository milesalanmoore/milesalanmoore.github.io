/.quarto/
.DS_Store
node_modules

# Build output — rendered in CI by .github/workflows/publish.yml and pushed to gh-pages
/_site/

# Quarto freeze cache: keep computation results, drop copied libs
/_freeze/site_libs/
